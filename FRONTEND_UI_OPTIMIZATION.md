# 前端界面优化（管理系统）

本文档针对「专业、克制、数据优先」的管理系统界面：少解释、多数据、少引导、多效率。仅调整视觉层级与交互一致性，不改变业务逻辑。参考成熟设计体系（如 Shopify Polaris 与 GOV.UK Design System）的通用规范。citeturn0search1turn0search2turn0search3

## 1. 视觉基调

- 基调：冷静、低饱和、偏中性。
- 主色：深蓝/石板蓝，仅用于关键按钮与强调。
- 辅色：灰蓝/浅灰，用于分隔与结构。
- 状态色：轻饱和，避免大面积高亮。
- 背景与表面：强调“背景/表面/重点元素”的分层，强调色仅用于小面积（按钮、标签）。citeturn0search1
  - 页面背景：`#F6F7FB` 或 `#F5F6FA`
  - 卡片/表面：`#FFFFFF`，边框 `#E6E8EF`
- 阴影：轻量化，仅用于层级区分。

## 2. 字体与间距

- 字号层级：
  - 页面标题：`18–20px`
  - 模块标题：`16px`
  - 正文/表格：`13–14px`
  - 次要信息：`12px`
- 行高：
  - 正文 `1.4–1.6`，表格 `1.2–1.4`
- 间距：使用固定网格 `8/12/16/24`，避免奇怪间距。

## 3. 导航与布局

- 侧边栏：折叠态保持窄宽度且不遮挡正文；展开态覆盖页面但不推动内容。
- 顶部栏：只保留核心操作（用户/刷新/退出）。
- 内容区：
  - 避免“说明性模块”，优先筛选与数据列表。
  - 单页不超过 2 种卡片样式。

## 4. 组件与列表

- 表格：
  - 表头轻分割线，不用深色表头背景。
  - 操作列只保留必要动作。
- 筛选区：
  - 单行或双行布局，不放引导性文字。
  - 占位符简短（例如“输入名称”）。
- 统计卡片：
  - 只显示“标签 + 数值”，不写解释句。
  - 数值加粗，标签弱化。

## 5. 页面建议

- 首页（仪表盘）：
  - 仅保留统计、通知、活动列表。
  - 去掉角色说明与“使用指南”类文字。
- 社团管理：
  - 只保留“总数 / 当前身份”。
  - 去掉加入逻辑说明。
- 费用管理：
  - 仅保留“收入 / 支出 / 余额 / 本次合计”。
  - 图表保持简洁（条形或占比）。
- 活动/报名：
  - 移除顶部提示，保留操作与列表。

## 6. 交互与状态

- 提示文案：
  - 移除说明性提示，仅保留错误/确认弹窗。
  - 确认文案简短（例如“确认删除？”）。
- 按钮：
  - 每个页面仅突出一个主按钮。
  - 文案统一“动词 + 对象”（如“新增成员”“删除活动”）。

## 7. 数据可视化

- 图表：
  - 一个图表只回答一个问题。citeturn0search0
  - 颜色不超过 2–3 种，强调色只用于对比。
  - 标题简短，避免叙述性句子。
- 汇总数据：
  - 仅保留“标签 + 数值”。

## 8. 一致性与可维护性

- 统一定义设计令牌：
  - 颜色（主色/中性色/状态色）
  - 间距（8/12/16/24）
  - 字体大小与行高
- 若使用 `less/scss`，在 `src/assets/styles` 增加主题变量文件。

---

## 参考设计体系（关键要点）

- Shopify Polaris：强调“背景/表面/强调”的色彩分层，数据可视化强调单一目的与清晰标注。citeturn0search0turn0search1
- GOV.UK Design System：强调一致性与可复用模式，适合低干扰、高可读的管理类产品。citeturn0search3

---

## 执行清单（优先级）

1. 统一页面背景、卡片表面与分割线颜色
2. 统一字体层级与间距尺度
3. 去除所有说明性 UI 文案
4. 简化顶部栏与侧边栏状态
5. 保持表格与筛选区布局一致
